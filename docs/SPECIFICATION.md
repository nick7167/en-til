# Implementation contract — 10 September 2026

The user's supplied implementation specification supersedes the discovery archive. All 26 concepts across six reference sheets are approved visual references. Native Danish iPhone iOS 18+, 3–8 players on individual internet-connected phones. SwiftUI, StoreKit 2, fresh TypeScript Workers + room Durable Objects + D1. No reused architecture. Portrait, Fraunces headings/system body, ink/violet/cream/lime, twelve original characters unique per room, illustrated lounge and winding board. Independent sound/haptics, reduced motion, Dynamic Type, VoiceOver, real keyboard accommodation. Working name pending clearance.

## Lobby and race
No sign-up; save identity/preferences locally and credentials securely. Four uppercase alphanumeric code accepts lowercase. Host receives code before editing. Names get stable playful duplicate suffixes without changing saved nickname. Eight admitted seats includes away/late players. Returning guests confirm identity. Lobby/name/character editing. Draft settings commit only via Gem ændringer; saving resets readiness; confirm discard. Guests ready; host starts after all ready.

Same question → acknowledged private answer lock → acknowledged backing of another active player. Multiple players may back same person. Keep question visible, hide own answer during backing. Others' answers/backing hidden until completion. Own correct = 1; backed person's answer correct = 1, independent of their backing. Move 0–2. Finish 10/20/30 or custom 5–50, default 20; score full round before finish check; preserve overshoot; furthest wins, equal furthest joint winners.

Timed default: answer 20 seconds (10–60 step 5), backing 10 (5–30 step 5), or untimed. Individual backing clock starts when answer confirmed or expires. Unconfirmed input not submitted. Missing answer still permits backing. Finish early when all required inputs close; completed parts can score. Waiting identifies only completion of both steps. Synchronized 7–8 second reveal: answer, choices/backing, points, movement; then scrollable board and expandable results. Calm rate-limited applause/laughter/surprise/side-eye reactions. Reversible readiness until three-second countdown. Rematch same lobby/code/eligible settings; early end no winner.

## Personal and interruption rules
Private yes/no/skip → submitted response count → guess total yes → back friend. Own response counts. Closest submitted guesses correct including ties. Skip still permits guessing/backing/scoring. Never expose individual private responses/skip identity. Aggregate inference possible; no absolute anonymity promise. Below three submitted responses: cancel without points/reveal, delete private data, substitute free factual question even personal-only.

Reconnect automatically, clocks continue, restore saved seat with credentials/internal room ID and current snapshot. Two fully missed timed rounds mark away. Host can mark away (including untimed), preserve confirmed choices and close unfinished inputs. Away retains score, excluded from subsequent rounds/backing. Return at boundary. Deliberate leave preserves seat. Resolve current round below three, then pause. Host transfer on deliberate leave or 30s disconnected to earliest connected active player. Lobby removal, match sitting out. Late arrivals next match. Expire after 30m everyone disconnected. Breaks between rounds. Reports never change score.

## Optional drinking
Host initially off, then remember preference. Fully submitted answer/guess AND backing: 2 points => 0, 1 => 1, 0 => 2 optional sips. Individual opt-out/skip freely, neutral dash in shared results. No consumption/proof/accumulation/readiness condition. Remember adult confirmation before adult packs/drinking; declining returns code entry; recheck setting changes.

## Content and commerce
750 original human-reviewed Danish questions: 150 free, 100 each Danmark facts, Film & TV facts, Isbryderen Let, Lidt for ærlig Personlig, Efter midnat Fræk, Uden filter Meget fræk. Free both formats/full matches. Factual comparison/before-after/true-false/over-under/four-choice; random difficulty. Personal experiences/habits/opinions/preferences, consensual adult stronger content, exclude harmful/abusive topics. No live factual API/poster dependency. Concise correct answer, details/sources separately. Approximately equal types when both selected. Paid-only allowed. Prefer host-unseen question IDs; recycle exhausted pool, no match history. Validated versioned files, reviewed publication, frozen match version. Nicklas reviews batches ~20.

Permanent non-consumables target 29 DKK, six-launch-pack bundle fixed target 99 DKK; actual StoreKit localized prices. Updates/corrections included, future packs separate. Show ownership/cheapest remaining route/no retroactive credit. Shop/setup purchase and restore. No ads/subscriptions/advantages/round sales. Host shares selected ownership. Transfer preserves match content; rematch new host ownership. Setup purchase selects draft only; saving applies. Retry delayed server verification, retain verified access. Verify Apple signature/chain/app/product/environment/validity/revocation. Isolate sandbox/production. Restore without app account.

## Server, operations, release
Versioned authenticated commands with unique request and match/round IDs; server authorization/phase/idempotency, tailored snapshots/acks, server clocks/scores. Persist before ack, no duplicate scoring after restart. No future/correct-before-reveal/private answers in unauthorized payloads. Delete identifiable personal responses after aggregation/scoring; no logs/analytics; provider backup retention documented separately. D1 versioned catalogue, purchases, reports, minimal aggregate joining/completion/purchase/error metrics. Reports reason/optional note.

Separate test/prod resources/secrets. Target 100 rooms/800 players tested. Limit creation/code attempts/reports/reactions. Monitor usage, expire rooms, emergency disable. Preserve compatible running matches/content. Existing paid Workers account is not a spending cap; no new spending authorized.

Prepare private GitHub backend/content checks and Codemagic native builds/tests/screenshots/signing/TestFlight, conserve free allowance, pin stable Xcode. No local Xcode required. Native visual foundation then factual match then personal/packs/store/settings/report/drinking, content review and support/privacy drafts for adrez.dev. Push/deploy/TestFlight/website/submission require explicit project permission.

Acceptance covers scoring/ties/overshoot/winners; every timeout/duplicate/stale/reconnect/restart; 3/8 players/full/late/away/host/expiry/reused code; personal skip/privacy/fallback/personal-only; purchase outcomes/restore/revoke/bundle/transfer; adult changes/opt-out/incomplete exemptions; keyboard/long names/eight players/small screen/Dynamic Type/VoiceOver/reduced motion/mute; all reference screenshots/Danish copy; load/rates/usage/recovery. Physical Nicklas iPhone and hosted sims initially, TestFlight real touch/haptics/audio/pacing, actual group quality separately. Before publication: name clearance, final content/Danish review, signing/store/prices/privacy/age rating/Apple review. Mockups do not establish approval.
