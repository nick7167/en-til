# En til? — implementation handoff

Updated 10 September 2026. The user explicitly authorized implementation of the supplied specification, superseding discovery-only status. Read [docs/SPECIFICATION.md](docs/SPECIFICATION.md) for the current contract. The original conversation summary is preserved at [docs/archive/DISCOVERY_HANDOFF.md](docs/archive/DISCOVERY_HANDOFF.md).

Native Danish iPhone app, iOS 18+, SwiftUI/StoreKit 2; fresh TypeScript Cloudflare Workers, authoritative room Durable Objects, D1. No shared code or infrastructure with earlier games. Six approved sheets (26 screens) are in design/references. Written pack intensities override inconsistent mockup labels. En til? remains a working name. Bundle ID/endpoint are development placeholders.

Implementation is in progress. Native home/join/lobby/question/backing/personal/reveal/board/finale/settings/shop/report screens, twelve scalable characters, generated lounge and six cover assets, Fraunces and sound cues are written. The backend has accountless sessions, persisted room coordination, private snapshots, verified-purchase/notification integration, and 29 engine/RPC tests. Real local HTTP/WebSocket, process-restart, and 100-room/800-socket checks pass. The first 20 development questions await review. The app compiled and three tests passed on hosted Xcode; screenshot review identified three UI defects fixed and verified in a green follow-up checkpoint (see docs/NATIVE_REVIEW.md). GitHub Actions runs manual unsigned checkpoints; Codemagic is reserved for later signing/TestFlight. Check README and docs/VERIFICATION.md for tested scope and remaining work. Content is DRAFT: 0 human approvals. Never claim 750 reviewed questions exist.

The user authorized creating/pushing repository https://github.com/nick7167/en-til and hosted verification on 10 September 2026, then selected GitHub Actions for routine native builds and explicitly authorized public visibility. Use free standard runners only. No deployment, TestFlight, website publication, App Store submission, external messaging or new spending is authorized. No delegation unless authorized. The current environment has Swift command-line tools but no full Xcode; SwiftUI/simulator verification requires hosted Xcode. Do not call source parsing a native build.

Current task: user requires all 26 screens to match approved visual references, including original wordmark/cast and edge-to-edge scenes. Explicitly authorized token-efficient subagents and local artwork extraction. Reference map: docs/REFERENCE_SCREEN_MAP.md. Full visual rewrite is in progress, not yet visually accepted. Newly extracted assets, compact native layouts, real purchase-confirmation view and 26-reference screenshot coverage are prepared. Await the new hosted build and comparison; do not call this pixel-perfect until visually verified.

Visual checkpoint update (12 September): seven clean full-screen reconstruction candidates replace the rejected background cleanup. Original extracted logo/cast/covers remain. Heading font now freezes Fraunces soft axes; larger cast/cards and round navigation updates await hosted screenshot verification. All 26 reference captures passed in run 34558033828, but its keyboard accessibility assertion failed; corrected locally. Do not claim pixel-perfect completion.

Native run 34696177984 passed all three tests at 92e7225. Its 26 captures are downloaded under ignored `build/visual-second`; paired comparisons A–F show remaining layout differences. Follow-up adds a clean board environment, broader native tiles, larger primary/backing controls, adult scene offset, finale stage spacing, compact stacked pack artwork, and improved keyboard-tutorial detection. Reference fixtures now show ready guests, a mid-race board and plausible finale scores; this presentation data does not replace engine tests. Fidelity is still under review.

Latest visual pass (12 September): native runs 34697301308, 34697872872 and 34710744987 passed. Compact quartet artwork was found missing in screenshot review and is now included; a literal SwiftUI asset-name guard runs in native CI. Upright lime host art comes from A02. Room codes, board markers and shop rows are larger. Full rooms now use three-column lobby cards and compact backing cards; new UI assertions and accessibility XXXL captures await the next build. Pause artwork is extracted directly from E03, and background artwork dims for accessibility text sizes. Personal correct-answer rows gain staged lime outlines. No pixel-perfect claim: remaining review includes the latest screenshots, small-device/long-name/VoiceOver/device checks, and real configured StoreKit prices.

13 September continuation: native run 34712298330 passed four tests at 108bcde (26 concepts, eight-player interaction checks, keyboard, accessibility XXXL captures). Artifacts/comparisons are in ignored `build/visual-seventh`. Ice artwork is now reference-derived at cbef712, preserving the original hat/pickaxe hero; regeneration and a hero-region preservation assertion pass. Screenshot review found large-text names wrapping inside three-column cards and tests accepting partially visible actions. The next build switches accessibility player grids to one column, separates navigation from scrolled content, and requires controls fully inside the screen. Visual parity is still not signed off.

Latest continuation (13 September):
- Latest verified app commit: **41f6de8**. Native run **34770264528 passed all four UI tests** (420.632 seconds): all 26 concepts including fixture-price checks, additional/eight-player screens, keyboard, and accessibility XXXL controls. Backend/content CI also passed this commit.
- Downloaded review: ignored `build/visual-fifteenth`; `comparison/A.jpg` through `F.jpg` preserve aspect ratios. C/D/E/F manually reviewed. Ice framing, 29/99 kr. fixture prices and top-right setup close icon are confirmed in native captures.
- `PackStore.priceLabel` uses actual StoreKit prices normally. DEBUG screenshot launches use formatted target prices and skip load/buy/restore. These are presentation fixtures, **not purchase verification**. Experimental SKTestSession, framework overrides, local StoreKit configuration and test plan were removed after failed hosted attempts.
- Setup/results previews use native sheets. Large-text captures confirm readable names and fully visible create, backing-confirmation and next-round buttons.
- Remaining visual gaps: room navigation controls crowd longer headings; setup/results sheet composition and results-table density differ from the reference. Small-device/long-name/VoiceOver/device review and pixel-perfect approval remain open. Do not blindly copy reference privacy/drinking content when it conflicts with the specification.
- `native-review` exports full-resolution JPEGs/text diagnostics: locally verified 8 MB versus 104 MB with intact dimensions and manifest links. Original PNGs/videos remain in `native-checkpoint`. Compare using `python3 scripts/compare-reference-screens.py <folder>`; `--stretch` reproduces older distorted comparisons.
- Price assertion failures now allow later captures to continue while failing the suite. No native run is pending.
- Local Swift parsing, Danish price formatting, asset/exporter/comparison/diff checks pass. No deployment, TestFlight, live StoreKit configuration or human content approval occurred.

14 September checkpoint in progress: terminal room recovery clears stale seats; board readiness is reevaluated after away/leave, with host sit-out controls on the board. Setup/results sheets and header spacing refined. Added a real native multiplayer test using the local Worker (three rounds, reopening, joint finale, rematch, leave/return); native verification pending. Backend checks and two-restart recovery passed locally.

19 September verified continuation:
- Pushed through `689ce32`: terminal room recovery; readiness reevaluation after away/leave; board host sit-out menu; sheet/header adjustments; fixed settings save footer; full 44-point leave target; shared leaderboard ranks for tied scores.
- Native CI now starts a real local Worker and uses ad-hoc simulator signing with simulator-only Keychain entitlements. Test bundles generate Info.plist metadata. No Apple account or distribution signing is configured.
- New live UI test drives a real three-player match with API participants, factual/personal answers, relaunch, overshoot/joint winners, rematch, deliberate leave and active-seat return. Catalogue answers live only in the test bundle.
- Runs 34839108876 and 35446456867 were FALSE GREENS: macOS Bash rejected an empty test-argument array before Xcode ran. Do not cite them as test evidence. Fixed with a nonempty argument array, result-bundle check and explicit TEST SUCCEEDED log guard. Local Bash validation passed.
- Run 34838249028 proved Keychain-backed joining, three rounds, reopening and a joint finale, but failed incorrect test expectations and the leave target; those were corrected. Full run 35446538527 passed at 689ce32: two contract tests and five UI tests (431.730 seconds), including shared ranks and leave/active-seat return. The workflow log explicitly confirms TEST SUCCEEDED.
- Actual captures from run 34787702815 are under ignored build/visual-sixteenth. All four existing visual tests passed there; the new live test failed secure storage in the unsigned simulator. Results sheet/header composition improved, but setup save was clipped (now pinned). No pixel-perfect claim.

Native review artifacts are saved under ignored `build/visual-nineteenth`; comparisons A–F were generated. C01 confirms the settings save action is pinned and fully visible; the live finale confirms three first-place ranks. Accessibility XXXL keeps the save action visible, but the long settings heading breaks awkwardly and the fixed explanatory footer occupies substantial height. Refine that composition in the next visual pass; do not call it pixel-perfect.

20 September continuation: timer centring assertions and all twelve immersive pack screens passed native run 35505917580; that run had a keyboard accessibility timing failure subsequently fixed and passed in focused run 35506613394. Final owned-pack spacing passed run 35524368558 at 931b539; screenshots inspected under build/visual-twentyfirst. Movement-stage board animation and final-position assertion now await native verification. Local pnpm check passes 30 tests, with 20 draft questions and zero human approvals. No device motion or pixel-perfect approval claimed.

Movement verification complete: focused native run 35525670758 at 8e7744d passed the additional-screen UI test (82.395 seconds); its log confirms TEST SUCCEEDED. Screenshot inspected under build/visual-twentythird: board visible and Nicklas at field 6. Together with the other five UI tests and two contract tests in 35524844518, current app changes have passing coverage. Native resource checker now rejects fixture JSON missing from Xcode resources; positive/negative checks pass. Real-device pacing, late-stage recovery motion and overall pixel-perfect acceptance remain open. Known follow-up: setup age-confirmation currently requires toggling drinking rules again; complete the draft action after confirmation and verify it.

21 September readability checkpoint: ac7a2fa passed full native run 35527839000 (two contract tests, six UI tests; TEST SUCCEEDED confirmed). All 54 exported captures inspected under build/visual-readability, including all concepts, packs, supporting screens and accessibility XXXL. Added opaque text backing, solid card/selected-control surfaces, readable disabled actions, full-opacity board numbers/subtitles, and read-only guest settings without disabled dimming. All 13 palette contrast checks pass at 4.5:1 or better. See docs/TEXT_READABILITY_REVIEW.md. No claim of completed physical-device/VoiceOver validation.

21 September playful-artwork checkpoint: a8f1f5a passed focused native run 35588640948 (all 26 references and four accessibility XXXL captures; executed-test log confirms TEST SUCCEEDED). All 30 captures inspected under build/visual-playful-final, with proportion-preserving reference comparisons. Higher-resolution logo/cast, continuous lounge/backing art, rounded supporting text and preserved Fraunces headings replace the rejected pixelated/boxed treatment. Solid control surfaces remain; standalone copy uses glyph shadows. Full run 35545264745 previously passed both contract tests and six UI tests including live multiplayer. Final review found private guidance crossing the bright hero; eb40493 moves it below the illustration, verified in native run 35590245122 attempt 2. Asset resolution and 13 palette contrast checks pass locally; palette checks do not prove contrast over art. Exact character-rendering approval, physical-device/VoiceOver checks, content approval and configured StoreKit remain open. No release-readiness or pixel-perfect claim.

Native run 35590245122 attempt 1 compiled but timed out launching the app in the reference test; the following large-text test stalled until the 30-minute job cancellation. No completed result bundle was exported. This is not passing evidence. Attempt 2 passed on the same eb40493 commit: reference test 395.731 seconds, large-text test 43.481 seconds, and TEST SUCCEEDED confirmed in the executed log. Final private-round capture inspected under build/visual-private-final/review/D01-private.jpg: guidance sits below the bright character, above the answer buttons, without a backplate.

21 September TestFlight preparation: owner authorized backend deployment and TestFlight upload, supplied Apple team JDW2B73SS5, registered dev.adrez.entil, and created app 6814572975. Codemagic API access is stored only in ignored .env.local; signing private key is ignored and stored encrypted in the app-scoped en_til_signing group. Beta Worker en-til-beta is deployed with fresh D1 database 914d1961-084d-4598-ac54-fe62a9855a3d. Real remote eight-player HTTP integration passed. Signed build/upload not yet confirmed. See docs/HOSTED_BUILDS.md for the new beta workflow and limitations.

22 September TestFlight upload confirmed:
- Commit ce8897c pushed; GitHub backend CI 35659541943 passed. Local pnpm check, native asset/contrast checks and remote beta WebSocket integration passed.
- Codemagic build 6ab1a6e7bf3071e76e8ab256 finished successfully, including signing, archive and publishing. Apple uploader reported `UPLOAD SUCCEEDED with no errors` at 2026-09-21 22:01:52 UTC.
- Downloaded signed IPA inspected locally: version **1.0 (2)**, bundle dev.adrez.entil, minimum iOS 18.0, correct dedicated beta endpoint. Embedded distribution profile matches team JDW2B73SS5 and app identifier; get-task-allow is false. Artifact/logs are ignored under build/.
- Upload is confirmed; Apple processing and visibility to internal TestFlight testers are not yet confirmed. Workflow uploads only, with automatic beta review/App Store submission disabled. No external tester invitations or public submission occurred.
- Next: confirm build 1.0 (2) finishes processing in App Store Connect app 6814572975, then physical-device multiplayer/VoiceOver checks. Draft content (20 questions, zero human approvals) and configured StoreKit purchases remain limitations.
